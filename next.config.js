/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i.redd.it',
      'github.com',
      'careers.publicissapient.com',
      'media.licdn.com',
    ],
  },
};

module.exports = nextConfig;
